import { Title } from '@/components/shared';

const ProjectCardTasksView = () => {
  return (
    <div className="project-card-content-container">
      <Title size="l" text="tasks" />
    </div>
  );
};

export default ProjectCardTasksView;
